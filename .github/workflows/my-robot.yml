name: My First DevOps Robot

on: [push]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Привет!"

  second_job:
    runs-on: ubuntu-latest
    needs: greet
    steps:
      - name: Final Step
        run: echo "Вторая задача запущена!"
